## Packages
date-fns | Date formatting for auctions
framer-motion | Smooth animations for bids and transitions
lucide-react | Icons (already in base but listing for completeness)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Authentication uses Replit Auth (useAuth hook already provided)
Colors: WindTre Orange (#FF6A00), Dark Blue (#1D2338)
